{
  "name": "zeptrack",
  "private": true,
  "description": "ZepTrack test tooling. The app itself has no build step.",
  "scripts": {
    "test": "node tests/run.js"
  },
  "devDependencies": {
    "playwright": "1.63.0"
  }
}
